import { ComingSoon } from "@/components/ComingSoon";

// M3 owns this file — see docs/Dayflow-Team-Plan.md §2.3 (Step 7).
export default function EmployeesPage() {
  return <ComingSoon title="Employees" owner="M3 — People & Payroll" />;
}
