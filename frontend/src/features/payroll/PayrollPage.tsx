import { ComingSoon } from "@/components/ComingSoon";

// M3 owns this file — see docs/Dayflow-Team-Plan.md §2.3 (Step 16).
export default function PayrollPage() {
  return <ComingSoon title="Payroll" owner="M3 — People & Payroll" />;
}
