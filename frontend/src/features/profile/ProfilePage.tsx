import { ComingSoon } from "@/components/ComingSoon";

// M3 owns this file — see docs/Dayflow-Team-Plan.md §2.3 (Step 8).
export default function ProfilePage() {
  return <ComingSoon title="Employee Profile" owner="M3 — People & Payroll" />;
}
